{
  "kind": "build_request",
  "title": "Fix “No” button so it never disappears and keep message + movement looping until “Yes! 💖”",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Update the Opening Screen so the “No” button never disappears and remains visible and clickable indefinitely, continuing to jump to a new random on-screen position (within viewport boundaries) on every click, no matter how many times it is pressed, and only stopping when the user presses “Yes! 💖” (i.e., when the Opening Screen unmounts).",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-1"
        ],
        "quotes": [
          "After pressing the “No” button multiple times, it eventually disappears, leaving only the “Yes! 💖” button visible. I do not want that to happen.",
          "The “No” button never disappears.",
          "It continues to move randomly within the screen boundaries every time it is clicked.",
          "It remains visible and clickable at all times.",
          "It loops infinitely in the same playful way, no matter how many times it is pressed.",
          "The looping behavior (both button movement and message changes) only stops when the user presses “Yes! 💖”."
        ]
      },
      "acceptanceCriteria": [
        "On the Opening Screen, repeatedly clicking the “No” button 50+ times never causes it to disappear or become unclickable.",
        "Each click on “No” results in the button moving to a new random position.",
        "The new “No” button position is always fully within the visible viewport (with padding) and never renders partially off-screen.",
        "Viewport changes (resize/orientation change) do not cause the “No” button to end up off-screen; it is re-clamped to a safe visible position.",
        "Pressing “Yes! 💖” transitions away from the Opening Screen and stops the “No” behavior because the Opening Screen is no longer active.",
        "All other Opening Screen visuals and behavior remain unchanged (styling, layout, animations, copy)."
      ]
    },
    {
      "id": "REQ-2",
      "text": "Update the changing message under “Will you be my Valentine? 💖” on the Opening Screen so it continues cycling through the existing NO_MESSAGES in an infinite loop while the user remains on the Opening Screen, and ensure this looping stops only when the user presses “Yes! 💖” (i.e., when the Opening Screen unmounts).",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-1"
        ],
        "quotes": [
          "The changing message under “Will you be my Valentine? 💖” also continues cycling in a loop indefinitely.",
          "The looping behavior (both button movement and message changes) only stops when the user presses “Yes! 💖”."
        ]
      },
      "acceptanceCriteria": [
        "After at least one “No” interaction triggers the alternate messages, the message continues to cycle through NO_MESSAGES and wraps back to the start without stopping while the Opening Screen is displayed.",
        "The message cycling does not stop after any number of “No” presses.",
        "When the user presses “Yes! 💖” and the app transitions away from the Opening Screen, message cycling stops (no timers/intervals continue running in the background).",
        "All user-facing text remains in English and uses the existing strings in constants.ts (no copy changes unless required to enable looping behavior)."
      ]
    }
  ],
  "constraints": [
    "Do not modify immutable frontend paths: frontend/src/hooks/useInternetIdentity.ts, frontend/src/hooks/useInternetIdentity.tsx, frontend/src/hooks/useActor.ts, frontend/src/main.tsx, frontend/src/components/ui.",
    "Keep everything else exactly the same aside from fixing the “No” button persistence/movement and the indefinite message cycling behavior.",
    "No backend changes are required for this fix."
  ],
  "nonGoals": [
    "Do not add new screens, routes, or backend features.",
    "Do not change the “Yes! 💖” behavior, transition timing, fireworks, hearts background, or final celebration screen content/layout.",
    "Do not change the wording of existing messages/buttons aside from implementing the requested looping behavior."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}